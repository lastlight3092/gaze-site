"use client";

export default function AnnouncementBar() {
  return (
    <div
      style={{
        background: "var(--espresso)",
        borderBottom: "1px solid var(--border)",
        textAlign: "center",
        padding: "10px 20px",
        fontSize: "11px",
        letterSpacing: "0.14em",
        textTransform: "uppercase",
        color: "var(--bone)",
      }}
    >
      Discreet packaging · Plain billing · Regional delivery across Southeast Asia
    </div>
  );
}
